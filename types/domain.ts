export type SectionProgress = {
  userId: string;
  groupId: string;
  programId: string;
  weekNumber: number;
  dayNumber: number;
  sectionId: string;
  completed: boolean;
  pointsEarned: number;
  updatedAt: string;
};
